/**
 * Scrollable grid body height on checklist job detail (header, tabs, toolbar, pagination).
 * Applied as height + maxHeight so the table fills the viewport like admin list pages.
 */
export const CHECKLIST_JOB_DETAIL_TABLE_MAX_HEIGHT = "calc(100dvh - 200px)"

/** List page: header, filters, status tabs, toolbar, and pagination */
export const TALLY_SERVICE_LIST_TABLE_MAX_HEIGHT = "calc(100dvh - 360px)"

/** Checklist list page — same chrome as tally service list */
export const CHECKLIST_LIST_TABLE_MAX_HEIGHT = "calc(100dvh - 360px)"
